// Code your solution in this file
const drivers = ['Bobby', 'Sammy', 'Sally', 'Annette', 'Sarah', 'Bobby']

function findMatching(drivers, driver) {
  driver = driver.toLowerCase()
  // do end {}
  const ans = drivers.filter(word => word.toLowerCase() == driver)
  console.log(ans)
  return ans
}
findMatching(drivers, "Sammy")

// function findMatching2(drivers, driver) {
//   const ans = drivers.filter(function (word) { return word == driver })
//   console.log(ans)
//   return ans
// }
// findMatching2(drivers, "Sammy")
function fuzzyMatch(drivers) {
  drivers = []
  if drivers.startwith? {
  console.log(drivers)
  }
}
fuzzyMatch(drivers)
